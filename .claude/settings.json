{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); [[ \"$f\" == *.ts ]] && cd /Users/linwgpeter/dev/side-projects/spots-list/.worktrees/backend && npx tsc --noEmit 2>&1 | head -20 || true"
          },
          {
            "type": "agent",
            "prompt": "A file was just written or edited. Check the modified file for any exported functions, classes, or methods. Then search the rest of the codebase to see if any of those names already exist in other files. If you find duplicates, report them clearly so the developer can consolidate. Only report actual duplicates — skip test files and index re-exports."
          }
        ]
      }
    ]
  }
}
